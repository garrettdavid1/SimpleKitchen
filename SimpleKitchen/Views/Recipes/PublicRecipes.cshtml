@model IEnumerable<SimpleKitchen.Models.Recipe>
<script src="~/Scripts/PublicRecipeDetailsScript.js"></script>
@{
    ViewBag.Title = "Public Recipes";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Personal Recipes", "Index", null, new { @class = "link-to-personal-recipes requires-authentication" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RecipeName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <button type="button" href="~/" id="@item.RecipeId" class="btn btn-default glyphicon glyphicon-floppy-disk save-recipe"></button>
                @Html.DisplayFor(modelItem => item.RecipeName)
            </td>
            <td>
                @Html.ActionLink("Details", "PublicRecipeDetails", new { id = item.RecipeId })
            </td>
        </tr>
    }

</table>